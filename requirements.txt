# Core: the extractor runs on the standard library alone.
# Each dependency below is OPTIONAL and only needed for a specific source/feature.

huggingface_hub  # src/download_corpus.py (fetch EDGAR parquet into ./data)
pyarrow          # --source parquet: (read local .parquet corpora)
datasets         # only if you still use the legacy --source hf: path

# Higher recall on dates/quantities via NER (see --use-spacy):
#   pip install spacy && python -m spacy download en_core_web_sm
# spacy          # uncomment to install
